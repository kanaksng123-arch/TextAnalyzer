spring:
  application:
    name: textAnalyzer
  data:
    mongodb:
      uri: mongodb+srv://kanaksng123_db_user:kashishsingh@cluster0.edybjhp.mongodb.net/?appName=Cluster0
      database: textAnalyzerDb
      auto-index-creation: true

server:
  port: 8080
  servlet:
    context-path: /api

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
